import 'package:flutter/material.dart';

class Testpath extends StatelessWidget {
  final BuildContext context;
  const Testpath(this.context);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("test "),
      ),
    );
  }
}
